# Database Connection String
# For docker, use the service name 'db' as host if running inside docker network, or localhost if running locally with port mapping.
DATABASE_URL="postgresql://postgres:password@localhost:5432/cms_db?schema=public"

# Redis Configuration
REDIS_HOST="localhost"
REDIS_PORT=6379

# JWT Secret for signing tokens
JWT_SECRET="supersecretkey"

# Application Port
PORT=3000
